(function ($w) {
  var utils = $w.iMethod.utils;

  var def = {
    template: null,
    title: null,
    buttons: []
  };

  var btn = {
    id: null,
    name: null,
    value: null,
    click: null


  };

  var dialog = function (tmpl, opt) {
    var params = utils.extend({}, opt);

  };

  $w.iMethod._.dialog.ext = {
    alert: null,
    confirm: null,
    warn: null,
    error: null,
    dialog: null
  };

})(window);